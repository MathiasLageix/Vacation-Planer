# Cerveau de l'agent
anthropic>=0.40.0

# Appels HTTP async vers les fournisseurs
httpx>=0.27.0

# Fournisseurs de voyage
amadeus>=12.0.0          # SDK officiel Amadeus Self-Service

# Stockage + scheduler (Phase 2, déjà listés pour éviter d'y revenir)
sqlalchemy>=2.0.0
apscheduler>=3.10.0

# Config / secrets
python-dotenv>=1.0.0

# Confort dev
rich>=13.0.0             # affichage CLI lisible (tableaux comparatifs)

# API web (Phase 3)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Outils dev (optionnels)
ruff>=0.6.0
black>=24.0.0
