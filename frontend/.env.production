# Production Railway — chargé automatiquement par Next.js en mode production.
# Valeur lue par le route handler app/api/[...path]/route.ts à chaque requête
# (runtime, pas build time). Peut être surchargée par une variable Railway.
API_URL=https://vacation-planer-production.up.railway.app
